<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.luiskik10.controlcuentas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyFirebaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControlCuentas">
        <activity android:name=".loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".form_reporte2" />
        <activity android:name=".ActividadFormReporte" />
        <activity android:name=".ActividadCredito" />
        <activity android:name=".ActividadDebito" />
        <activity android:name=".ActividadCategoria" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".ActividadCuenta" />
    </application>

</manifest>